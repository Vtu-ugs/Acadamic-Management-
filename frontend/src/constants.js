// Reservation categories, shared by the Students personal-details form and the
// Admissions form. Admissions copies a student's category into actual_category,
// so the two must offer the same options or a copied value becomes unselectable.
export const CATEGORIES = ['General', 'OBC', 'SC', 'ST', 'Cat-1', '2A', '2B', '3A', '3B', 'Others'];
